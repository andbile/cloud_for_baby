{{#sprites}}
.icon-s-{{name}}:before {
  display: block;
  background-image: url(../img/{{{escaped_image}}});
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
}
{{/sprites}}